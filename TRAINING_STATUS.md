# A/B/C Run Status (Pointer)

This file is intentionally minimal to avoid stale duplicated run-state docs.

Use these as the authoritative sources:

- `PHASE_5_COMPLETION_CHECKLIST.md` for process/checklist and finalization steps
- `python3 python/watch_full_runs.py --log runs/full_abc_runs.log` for live status/ETA
- `runs/variant_*/results.json` for authoritative per-variant outputs

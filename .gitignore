# Lake build system
.lake/
lake-manifest.json

# Lean build artifacts
*.olean
*.ilean

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
venv/
.env

# Training runs — model weights are large; commit results.json only
runs/

# Temporary files
*.tmp
*.log
*~
nohup.out

# Local backup artifacts — regenerable, not tracked
Corpus/*.bak
datasets_phase5_backup/
datasets_phase5_v1/

# Local Ollama model definitions — not part of the repo
Modelfile

# SSH keys and credentials — never commit
*.key
*.pem
*_key
*_key.pub
authorized_keys

# Runtime PID files from training launchers
*.pid

# Reorganization 2026-08-09: stale root/doc leftovers, cannot delete on disk
/CORPUS_SCHEMA.md
/doc/
/Modelfile.tmp
nohup.out

# Stale dataset dirs (archived content; canonical datasets live in datasets/)
datasets_flat/
datasets_phase5_backup/
datasets_phase5_v1/

# Logs and runtime artifacts
logs/*.log
logs/*.pid
*.pid
*.tmp

# Intentionally-kept local dirs, not part of the repo
local-models/
ir-research/
/check_phase5_status.sh
/run_abc.sh
/elan-init.sh
docs/scratch/resume_notes_v2.md

# Local-only environment reference (never commit)
AGENTS_LOCAL.md
remote_check.py
apply_fixes.py
fix_polarity.py
fix_docstring.py
apply_corpus_builder.py
apply_per_operator.py
apply_python_per_operator.py
fix_decoder.py
fix_polarity_test.py
fix_proj_test.py
fix_projection_test.py
fix_bucketmode_threading.py
fix_trace_sig.py
gate4_vocab_check.py
fix_decoder_tests.py
fix_in_quotes.py
fix_output_tokens.py
fix_gen_decoder.py
fix_validate_roundtrip.py
fix_in_many.py

# Maith IR Test Suite

Canonical test status, counts, and decoder/golden coverage details are tracked in:

- `docs/TESTING_SUMMARY.md`

## Running tests

```bash
cd ~/Projects/Maith
lake build tests
./.lake/build/bin/tests
```

For experiment run/artifact validation tooling, see `docs/PYTHON_PIPELINE.md`.

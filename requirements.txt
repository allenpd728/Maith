# Python dependencies for Maith's ML pipeline (python/)
# Install with: /usr/local/bin/python3 -m pip install -r requirements.txt

# Core ML
torch>=2.0
transformers>=4.40

# Numerical
numpy>=1.24

# Probing / retrieval metrics (probing_task.py, retrieval_eval.py)
scikit-learn>=1.3

# Dataset / corpus handling (already stdlib, listed for clarity)
# json, pathlib, collections — stdlib
